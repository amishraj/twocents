# TwoCents – Claude Working Instructions

## Branch policy
- **Always work directly on `main`.** Do not create feature branches or worktrees unless the user explicitly asks for one.
- Before making any changes, confirm the working directory is the repo root (not a worktree).
- Commit directly to `main` when asked to commit.
